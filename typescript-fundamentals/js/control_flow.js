"use strict";
for (let i = 0; i <= 10; i++) {
    if (i % 2 === 0) {
        console.log(`${i} - even`);
    }
    else {
        console.log(`${i} - odd`);
    }
}
//# sourceMappingURL=control_flow.js.map